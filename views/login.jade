extends layout 

block header

block content
  div(class="container")
    form(class="form")

        div(class="mb-3")
            label(class="form-label" for="inputUsername") Email
            input(id="inputUsername", name="inputUsername", type="text", class="form-control" placeholder="email@example.com")

        div(class="mb-3")
            label(class="form-label" for="inputUsername") Password
            input(id="inputPassword", name="inputPassword", type="password", class="form-control" placeholder="password")
        div(id="passwordHelpBlock" class="form-text") Your password must be 8-20 characters long, contain letters and numbers, and must not contain spaces, special characters, or emoji.

        div(class="mb-3")
            input(type="checkbox", class="form-check-input", )
            label(class="", for="") Remember me

        input(id="inputSend", name="inputSend", type="submit", value="Login" class="btn btn-primary")    

block footer